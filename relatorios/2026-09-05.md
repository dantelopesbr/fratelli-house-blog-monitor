# Relatório — 2026-09-05

## Falha de rede

A rodada de hoje não conseguiu executar: o acesso a `blog.fratellihouse.com.br` está bloqueado pelo proxy de egress deste ambiente (`EGRESS_BLOCKED` / `connect_rejected`, gateway respondeu 403 ao CONNECT), tanto via `curl` quanto via `WebFetch`. Mesmo problema já registrado na rodada de 04/09/2026.

Nenhum dado foi coletado. `capas/checados.json` e `erros.md` **não foram alterados** nesta rodada, pra não estragar a baseline de comparação da próxima execução.

Estado atual do backlog (herdado da última rodada bem-sucedida, 03-04/09): 1.027/1.027 posts checados, restando 1 título quebrado (`/legenda-instagram/`, aguardando confirmação de rebuild) e 1 página com corpo vazio (`/escolher-sofa/`) — ver `erros.md`.
