# Relatório — 2026-09-16

## Falha de rede

A rodada de hoje não conseguiu executar: o acesso a `blog.fratellihouse.com.br` continua bloqueado pelo proxy de egress deste ambiente (`connect_rejected`, CONNECT rejeitado com 403 — "policy denial or upstream failure", confirmado em `$HTTPS_PROXY/__agentproxy/status`). Mesmo problema das rodadas de 04 a 15/09 — este é o **décimo terceiro dia consecutivo** de falha.

Nenhum dado foi coletado. `capas/checados.json` e `erros.md` **não foram alterados** nesta rodada, pra não estragar a baseline de comparação da próxima execução.

Estado atual do backlog (herdado da última rodada bem-sucedida, 03/09): 1.027/1.027 posts checados, 0 sem capa pendente. Resta 1 item aberto: página com corpo vazio em `/escolher-sofa/` — ver `erros.md`.

Nota operacional: o `HEAD` do repositório estava novamente "detached" no início desta rodada, 7 commits atrás de `origin/main` (relatórios de 09/09 a 15/09 já estavam em `origin/main` mas não no checkout local). Corrigi com `git fetch origin main && git checkout main && git merge --ff-only origin/main` antes de prosseguir — nada foi perdido. Mesmo problema relatado nas notas de 11 a 15/09; ainda não resolvido na infraestrutura do job.
