# Relatório — 2026-09-12

## Falha de rede

A rodada de hoje não conseguiu executar: o acesso a `blog.fratellihouse.com.br` continua bloqueado pelo proxy de egress deste ambiente (`EGRESS_BLOCKED` / `connect_rejected`, CONNECT rejeitado com 403), tanto via `curl` quanto via `WebFetch`. Mesmo problema das rodadas de 04 a 11/09 — este é o **nono dia consecutivo** de falha.

Nenhum dado foi coletado. `capas/checados.json` e `erros.md` **não foram alterados** nesta rodada, pra não estragar a baseline de comparação da próxima execução.

Estado atual do backlog (herdado da última rodada bem-sucedida, 03/09): 1.027/1.027 posts checados, 0 sem capa pendente. Resta 1 item aberto: página com corpo vazio em `/escolher-sofa/` — ver `erros.md`.

Nota operacional: o `HEAD` do repositório estava novamente "detached" no início desta rodada (2 commits à frente do `main` local, mas idêntico a `origin/main`). Corrigi com `git checkout main && git merge --ff-only origin/main` antes de prosseguir — nada foi perdido. Isso já é a segunda rodada seguida (11/09 e agora 12/09) em que isso acontece; vale investigar por que a rodada anterior não deixa o `HEAD` apontando pra `main` antes de encerrar.
